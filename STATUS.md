# MCH Studio — Status

## What happened
- Renamed from Command Center to MCH Studio
- Stripped all CRM features (pipeline, contacts, follow-ups, call logs) — those live in Arc now
- Removed the old content queue, calendar, preview/approve routes, content API route, and legacy content components
- Deployed to https://mch-studio.vercel.app

## Direction (2026-04-29)
- MCH Studio is the Codex-front-runner project for social, marketing, ads, and video production systems
- Current target workflow: raw idea → saved post bundle → generated image → manual review/publish
- Claude Code remains coordinator for vault/business continuity; Codex leads bounded implementation here
- Keep the system workflow-first. The app is optional scaffolding, not the source of truth.
- Working split: UI lane for Studio interface updates; social/ads lane for priority post bundles and generated images; practical video lane for separate recording/script planning.

## Recent
- 2026-05-29 — Created a new `free-road-map` Studio draft bundle for an MCH Projects base ad inspired by the MCH Projects site `/design-overlay` route. Added organic + hard-ad copy to `studio-runs/posts.json` and rendered matching light systems-design assets at `studio-runs/media/free-road-map/{organic,hard_ad}.png` from editable SVG sources. The hard ad frames a free road map as the first low-friction offer for finding the first system worth fixing.
- 2026-05-29 — Created the `late-night-headaches` Studio draft bundle from the "what keeps you up late at night" ad idea. Generated a male/female late-night business-owner source image with Codex image generation, then composed MCH Projects design-overlay-style SVG/PNG assets at `studio-runs/media/late-night-headaches/` for organic and hard-ad variants. The hard ad uses the two offer paths: digital systems for organization headaches and digital marketing for growth/leads.
- 2026-05-20 — Created a standalone anonymized client-intake case study at `studio-runs/case-studies/cpq-tier2-email-case-study.html`, then revised the copy into plainer English and refocused the theme around cleaner automated workflows saving real client-business time and reducing errors. It is a medium/long-form HTML document with a standardized case-study structure, CSS-only graphics for before/after process, benefit overlap, and Tier 1/2/3 roadmap positioning. This is intended as a reusable case-study asset for future Studio/social/ads direction, not as a current app feature.
- 2026-05-07 — Clarified Ads Simulator vs Social separation. Social remains the practical MCH production lane for real saved bundles and publishing review. Ads Simulator is now a standalone practice lane: challenge cards for Bella Vista Restaurant, TaskFlow Pro, and SkillMaster Academy; each opens a sandbox campaign/ad set/ad workflow with editable fake ad copy and validation checks. The ads practice data no longer pulls from or depends on social post bundle assets.
- 2026-05-06 — Pivoted the `mch-projects-biz-intro` bundle from website-ownership framing toward MCH Projects' newer automation positioning. Context came from `mch-projects-site`: lead follow-up, onboarding automation, automation leak map, and owner-controlled workflows. Replaced raw/screenshot-led visuals with branded graphic SVG assets (`automation-system.svg`, `automation-leak-map.svg`) that include the MCH Projects wordmark and show the lead-to-onboarding mechanism.
- 2026-05-06 — Reworked `/studio/meta-ads-simulator` toward the Rich UX reference flow while keeping MCH Studio's brutalist visual style. Saved the provided screenshots under `studio-runs/reference/meta-ads-simulator/`. The simulator now behaves more like an Ads Manager workbench: campaign table, ad set table, ad table, create/edit navigation, and an edit-ad modal with copy fields, UTM, image selection, Facebook/Instagram tabs, and a live ad preview.
- 2026-05-06 — Reworked the `mch-projects-biz-intro` draft into a stronger site-redesign/ownership post bundle. Organic copy now frames the MCH Projects site rebuild as "owned, not rented" and sets up a month of screenshot/teardown/talking-head content instead of a generic launch post. Replaced the generic AI visuals with screenshot-based site assets captured from `mchproj.com`, including a harder ad-style ownership graphic. Added `studio-runs/library/mch-site-redesign-series.md` as a lightweight 30-day content lane.
- 2026-05-06 — UI lane pass: added a small production board to `/studio` and `/studio/saved`, grouped saved bundles by status, and surfaced image-pass callouts for bundles missing variant images. Shared post UI helpers now keep status styles, excerpts, variant lookups, and bundle counts consistent without adding generation controls to the app.
- 2026-05-06 — Split active Studio work into three lanes in the docs: UI changes, priority social/ad-style bundle production, and separate practical video planning. This keeps the current social workflow moving while leaving room for app polish and future real-camera/screen-recording formats without reviving heavy video systems by default.
- 2026-05-06 — Reframed Ads Simulator to match Rich UX course framing instead of fake performance forecasting. Removed spend/clicks/purchases/ROAS/CPM/CPC/projected-value metrics. The page now simulates Meta setup structure: Campaign (objective, CBO/ABO, daily/lifetime, bid strategy), Ad Set (conversion location, dynamic creative, audience, placements), Ad (image count, primary texts, headlines, description, UTM tracking), plus a validation board and Rich UX creative reminders.
- 2026-05-06 — Simplified the Social page back toward the intended local-first Studio role. Removed the in-app idea text input and optional advanced-direction box; `/studio` now shows the terminal-driven workflow, a static prompt example, the saved social bundle list as actual rows, and a bottom note explaining how Ads Simulator practice informs hard-ad variants. Generation/refinement remains in the terminal.
- 2026-05-06 — Applied the clean Ads Simulator look across the whole Studio surface: global theme tokens, top nav, Social creator, Saved bundles, bundle previews, login/settings UI primitives, and favicon now use white/off-white surfaces with ink controls and warm yellow accents/shadows instead of the earlier green-tinted Studio palette.
- 2026-05-06 — Retuned the Ads Simulator palette away from the green Studio tint toward the cleaner original ad-tool feel: white/off-white background, ink controls, warm yellow accents, and yellow panel shadows. Removed remaining green accents from the simulator surface while leaving the Social page unchanged.
- 2026-05-06 — Renamed the nav item to "Ads Simulator" and restyled `/studio/meta-ads-simulator` into the same light cream/white Studio theme instead of the older dark simulator shell. Social now presents the lightweight workflow as three steps (prompt example → review/refine text → refine visual asset) with optional advanced direction, while keeping the actual generation flow local-first.
- 2026-05-06 — Corrected the Studio IA: `/studio` is the streamlined Social post creator, `/studio/meta-ads-simulator` remains its own full simulator page, and `/studio/saved` holds generated bundles. Social generation now stays basic by default (one idea + optional advanced notes) while borrowing Meta Simulator discipline for the hard-ad variant: sharper hook, visible proof, cleaner visual, and CTA shape. Bundle previews at `/studio/p/[id]` keep the more realistic organic/sponsored social post frames with profile chrome, image framing, engagement row, and ad CTA treatment.
- 2026-05-05 — Clarified the Studio generation docs so image generation is environment-aware: Codex should use Codex image generation, while Claude Code should use the Gemini recipe from `~/.claude/skills/add-visuals/SKILL.md`.
- 2026-05-05 — Generated a new Studio draft bundle from the idea "mch projects biz intro / site on linkedin see: mchproj.com." Added `mch-projects-biz-intro` to `studio-runs/posts.json` with organic + hard-ad variants, both grounded in the Marketing Toolkit / Client Acquisition notes, and generated Gemini image assets at `studio-runs/media/mch-projects-biz-intro/{organic,hard_ad}.png`.
- 2026-05-05 — Simplified Studio UI/copy around the current post-bundle workflow. Hub language now centers on "idea → saved bundle → preview → manual publish" instead of terminal mechanics. Top nav is reduced to Hub + Saved Bundles, hiding Settings/Sign out from the active surface. Saved page is now "Saved bundles." Preview cards now include copy buttons for organic/hard-ad variants and clearer "No image generated yet" placeholders.
- 2026-05-05 — Matt clarified the strategic pivot: shelve the heavier idea → execution → review → production-run workflow as the default, and keep MCH Studio simple/local for now. Current target is one idea → generated text variants → generated image(s) → saved local preview. Remotion, AI video generation, and fancy motion systems are deprioritized unless explicitly revived. Future video focus should be organic: selfie/talking-head recordings and Loom/OBS-style screen recordings for LinkedIn, YouTube, or Shorts. Meta Ads Simulator may eventually be integrated, but hold off for now. Updated docs to reflect this lightweight post/image direction.
- 2026-05-05 — Third pivot: brought back the Hub. `/studio` is now a one-sentence-idea launchpad with three step cards (drop idea → compose terminal prompt + Copy → review in Saved); recent-3 row at the bottom links into the saved list. Posts list moved to `/studio/saved` with statuses `draft / ready / published` (dropped `killed`). Schema in `src/lib/posts.ts` extended to bundles: `{ id, idea, title, variants[{type, body, image?, cta?}], status, updated_at }` with a backward-compat shim. Preview at `/studio/p/[id]` now renders organic + hard_ad side-by-side with image support, idea sentence, and a Step 3 ship-it footer that references `scripts/posts.sh publish <id>`. Added `/api/media/[...path]` to serve images out of `studio-runs/media/` with a path-traversal guard (verified — `..` access returns 404). Added `scripts/posts.sh list|ready|publish|draft <id>` (jq-backed, atomic via tmp file) — list output verified. New `/studio-generate` skill: source-of-truth spec at `studio-runs/_skills/studio-generate.md` (vendor-neutral, read by Codex/Gemini via 1-line pointer in AGENTS.md + CLAUDE.md), packaged Claude Code skill at `~/.claude/skills/studio-generate/SKILL.md` defers to that file. Workflow: read Marketing Toolkit kbs, optionally pull a Rich UX course, draft both variants, generate one image per variant (Codex image gen in Codex, Gemini in Claude Code), write bundle to posts.json, print preview URL. Topbar nav: Hub · Saved. `pnpm lint` + `pnpm build` pass; dev server returns 200 on /studio, /studio/saved, /studio/p/<id>, and 404 on traversal attempts and unknown media. `meta-ads-simulator` route still on disk pending explicit deletion.
- 2026-05-05 — Second pivot per Matt's verbal direction. Stripped the Boosted (Meta Ads) simulator from the Studio surface: not in nav, not on home (route still on disk at `/studio/meta-ads-simulator`, treat as separate tool). Deleted the platform-tabbed Social Simulator (`src/app/(dashboard)/studio/social-simulator/*`) — too many controls. New flow is CLI-first: terminal (Codex/Claude Code) writes posts to `studio-runs/posts.json`; the app is a list + general preview only. Built `src/lib/posts.ts` (server reader + `Post`/`Media`/`PostStatus` types). New `/studio` page is the Posts list (Step 1) with status pills, image/video badges, last-updated. New `/studio/p/[id]` is the general preview (Step 2 + Step 3 ship-it footer) — supports text + image + video media in a single layout. TopBar restyled cream `#ededdf` to match. Posts seeded with 3 sample entries. `pnpm lint` + `pnpm build` pass; dev server (3001) returns 200 on `/studio`, 200 on `/studio/p/<id>`, 404 on unknown ids.
- 2026-05-04 — Pivoted Studio surface to "early Meta Ads Simulator" cream/white aesthetic per Matt's verbal direction (Codex out of tokens). Built `/studio/social-simulator` (`src/app/(dashboard)/studio/social-simulator/{page,SocialSimulator}.tsx`): prompt-driven organic post composer with platform tabs (LinkedIn / Instagram / X), five angle presets (Day in the life / Announcement / Quick tip / Founder POV / Proof), live preview with platform chrome, hard-shadow cream panels, no AI/API calls. Rewrote `/studio` home as a two-card signpost (Social = Organic, Meta Ads = Boosted), same light look. TopBar nav now: Home · Social · Boosted. `pnpm lint` + `pnpm build` pass; dev server (port 3001) returns 200 on both new routes with no runtime errors. Existing `/studio/meta-ads-simulator` intentionally kept as the "Boosted" path for now.
- 2026-05-03 — Researched Remotion polish + audio options. Wrote `remotion/PLAN.md` (Codex-primary, Claude on-call coordination). Filed full research at `vault/Reference Materials/Remotion/Polish & Audio Research.md` and Codex's split advice at `vault/Advisors/codex/remotion-polish/`. Drafted audio cue sheet at `studio-runs/2026-04-workflow-first-mch-studio/assets/audio-cue-sheet.md`. Audio stack decided: Phase A free (OpenAI `gpt-4o-mini-tts` voice `onyx` OR self-recorded; YouTube/no-copyright-music.com/Pixabay for music; Mixkit + FILM CRUX for SFX). Phase B (ElevenLabs paid) triggered only when Matt explicitly says ready. Outro VO line dropped. Made `remotion/PLAN.md` discoverable to Codex via 1-line addition to AGENTS.md + CLAUDE.md. Codex prompt drafted; ready to fire phase 1 (`src/lib/theme.ts` + `motion.ts` + `timing.ts` + canonical refactor)
- Shifted Studio to a workflow-first local file system
- Added `studio-runs/` with inbox, reusable templates, library files, and a complete starter production run
- Updated the workflow so every publish kit requires a visual attachment: Remotion MP4 preferred, image/photo fallback allowed
- Added a Remotion/Tailwind video project under `remotion/`
- Rendered the starter run attachment at `studio-runs/2026-04-workflow-first-mch-studio/exports/marketing-production-rhythm-remotion.mp4`
- Rejected the first Remotion render as too static; the workflow now requires visual assets to pass a creative quality gate before publish readiness
- Added a separate Codex Remotion candidate composition, `MarketingProductionRhythmCodex`, for comparison against Claude's redesign pass
- 2026-05-03 — Completed Remotion polish phase 1 primitives: added `remotion/src/lib/theme.ts`, `remotion/src/lib/motion.ts`, and `remotion/src/lib/timing.ts`; refactored canonical `remotion/src/CodexMotionConcept.tsx` to consume the shared theme, tween/clamp helper, and timing constants without concept changes. `pnpm lint` and `pnpm build` pass; `pnpm dev` reached Remotion Studio server-ready, then the local watcher hit `EMFILE` in this sandbox.
- 2026-05-04 — Fixed Remotion Studio startup on the local sandbox: `pnpm dev` now preloads `remotion/scripts/watchfile-shim.cjs`, uses polling watcher env vars, disables auto-open, disables Webpack lazy compilation, and ignores build output. Verified Studio stays running at `http://localhost:3000/MarketingProductionRhythmCodex` with no browser console errors; `pnpm lint` and `pnpm build` pass.
- 2026-05-04 — Integrated Matt's creative feedback into `vault/Reference Materials/Remotion/Polish & Audio Research.md` with fresh TikTok/Shorts/Reels best-practice research. Reworked canonical `MarketingProductionRhythmCodex` into a cleaner mobile-first motion graphic: fewer words, much larger type, stronger visual metaphors, and more structural transitions. This intentionally superseded the old phase order because the review changed the creative direction. Verified in Remotion Studio with no browser console errors; `pnpm lint` and `pnpm build` pass.
- 2026-05-04 — Recreated the original Meta Ads Simulator idea inside Studio at `/studio/meta-ads-simulator`: local interactive planning sandbox for objective, audience, funnel stage, creative angle, budget, flight length, forecast metrics, launch checks, and an ad preview. Wired it into the Studio page and top nav. No database or AI/API surface added.
- 2026-05-04 — Claude alt-perspective pass per `remotion/PLAN.md` coordination model. Wrote treatment doc at `remotion/TREATMENT-CLAUDE-ALT.md` ("Counter Cut" — kinetic-counter through-line, single persistent rhythm bar, ≤4 words/scene, hero type 220–280px, ink/cream/green palette + one amber hit). Built sibling composition `MarketingProductionRhythmAlt` at `src/MarketingProductionRhythmAlt.tsx` (six `<Series.Sequence>` scenes, root-level `RhythmBar` driven by global frame, S3→S4 cream-wipe color flip). Registered in `Root.tsx` and added `pnpm render:alt`. Canonical `CodexMotionConcept.tsx` untouched. `pnpm lint` and `pnpm build` pass — alt + canonical render side-by-side for A/B.
- 2026-05-03 — Rewrote `remotion/src/Composition.tsx` as a five-beat motion graphic (scatter idea cards → sorted lanes → weekly rhythm rail → LinkedIn post mockup → kinetic tagline). Rendered `exports/marketing-production-rhythm-remotion-v2.mp4` (1080x1920, 30s, 6 MB). Lint + tsc clean. Updated run files (`media.md`, `tasks.md`, `publish-kit.md`, `log.md`); awaiting Matt's visual-quality-gate sign-off
- Replaced the browser pack generator with a lightweight signpost page
- Removed legacy queue/calendar/content review UI and content API routes
- Kept app/auth plumbing only as optional future scaffolding

## Current state
- Moved to `~/claude/mch-studio/` (standalone, out of mch-projects)
- GitHub: https://github.com/mattchx/mch-studio
- Deployed to https://mch-studio.vercel.app
- Auth is **temporarily disabled** — app is open access, /login redirects to /studio
- Dev server runs on port 3001
- `studio-runs/` is the source of truth: `/studio` is now the lightweight Hub/Saved preview surface, old review routes are gone, and `studio-runs/posts.json` is the active post-bundle store.

## Next steps
- Run three real ideas through the simple `posts.json` bundle workflow before adding app/database features
- Decide whether the Hub needs a prompt text area or clearer step-by-step instructions; do not complicate it yet
- Keep UI changes, social/ad production, and practical video planning in separate chats/lanes unless intentionally combining them for a bounded task
- Keep Remotion shelved unless Matt explicitly revives it
- Keep Meta Ads Simulator separate until Matt decides whether it belongs inside Studio
- Collect reusable hooks/angles into `studio-runs/library/`
- Re-enable auth only if the app becomes useful again
- Clean up old DB tables (follow_ups, call_logs, contacts) from Command Center era
- Delete old `command-center` Vercel project
